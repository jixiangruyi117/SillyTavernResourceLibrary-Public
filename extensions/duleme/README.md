# 读了么 0.1.13 内置 APP（本地候选）

0.1.13：聊天包默认只保存聊天，头像与角色显示正则作为聊天的阅读资料随备份携带，不新增完整角色卡。已有内容完全相同的卡需居中确认后关联，也可使用随附资料；没有对应卡时可显式同时保存角色卡。手动 JSONL 仍需绑定卡。需同步新版资源库宿主。

0.1.11 修复多个状态栏先后载入时抢走当前阅读页的问题；切换排版不再等待存储完成才刷新，也不会重新打开已关闭的设置。完整阅读对有明确零高度收起规则的 max-height 折叠动画取消展开上限，避免多人同时展开被截断；作者的收起状态与独立滚动区域保留。需同步本版配套资源库宿主。

0.1.10 修复酒馆排版下，整楼 HTML 开场白被头像/名字挤到第 3 页的问题：开场与楼层头整体进入同一长页，正文分页不变。交互页面加载期间保留可读静态内容，收到实际布局后原位切换，离开后返回也可先读静态内容；外部字体、图片或脚本的网络耗时仍取决于来源。无需新的宿主 SDK，可沿用 0.1.9 配套资源库。

0.1.9 在“楼层菜单 → 变量变化回顾”查看当前回复的已保存快照与此前最近一份快照的差异，可切换上下楼、返回本楼原文或对比楼层。MVU 比较 stat_data，显示新增、移除、前后值与数值增减；普通变量对象也可比较。首次快照、缺失快照、类型不兼容分别提示；中间有楼层缺失时明确标注，不能把全部变化归因于当前楼。切换回复只读取该版本的存档，不重算后续楼层，不重放 MVU，不补猜历史值。

此功能需要 0.1.9 配套资源库宿主。打开面板才读取原件，不另存变量副本；每次最多展示 200 项差异，比较深度与节点数有界，超出明确提示。长值仅显示前 500 字，新增/删除的整组对象或数组显示项数。路径和值遵循阅读打码；不发送给 AI 或外部服务。

0.1.8 在“阅读外观 → 外观配置”选择“跟随默认外观 / 角色专属外观”。首次启用专属时复制当前主题、美化 CSS、资源库美化引用、字号、行距、排版、状态栏配色/背景融合及正则总开关；随后修改自动保存到该角色。同一角色卡绑定的聊天共用，其他角色不受影响。切回默认保留专属方案；“重置角色外观”清除方案并恢复当前默认配置，不清除聊天、备注、收藏或进度。

角色专属正则方案需要 0.1.8 配套宿主：按规则内容与来源识别，全局/预设/角色卡规则只有内容一致才复用开关，规则缺失或改变时沿用归档设置。旧版按聊天保存的开关保留，跟随默认时继续使用。远程媒体、脚本、身份打码与阅读方式仍为原有通用设置，不随角色外观自动开启；本地手选背景图片仍仅当前阅读有效，不能持久保存。

读了么现注册为资源库内置 APP，沿用原测试扩展的数据标识，需配合本版资源库运行。0.1.7 的字体导入、状态栏跟随正文配色，以及此前的回复比较和正则缓存需要配套宿主构建，仅覆盖 APP 包不能补齐功能。旧正式站还没有聊天分类和对应 SDK。本轮没有部署或修改正式站点。

1. 在配套资源库导入角色卡（PNG/JSON）和酒馆导出的 JSONL 聊天记录；也支持严格的聊天消息 JSON 数组。使用 Bridge `0.3.36-chat.4` 测试扩展时，可在资源库互传页点“读取聊天记录”后取回，或在酒馆“加密暂存 → 发给资源库 → 读取已保存聊天”勾选暂存，再由资源库领取。两种方式都随附所属角色卡、全局及当前所选预设的显示正则，并自动绑定；功能桌面没有互传时，先在 APP 管理下载它。
2. 在“功能 → APP 管理”下载读了么，随后从功能桌面打开。旧测试版的备注、收藏、进度和外观直接保留，不需要重新导入扩展包；新用户也不需要选择信任模式。完整模式和远程媒体仍需在阅读设置中自行开启。
3. 手动导入的聊天在“待绑定”选择实际对应的角色卡，然后按“角色 → 聊天记录”阅读。互传归档自动关联随附卡，同名不会作为身份依据。
4. 右上角图片 SVG 开关控制角色聊天列表底图；毛玻璃效果使用角色缩略图，无图保持普通背景。正文背景另行设置。
5. 阅读工具里可切换上下滚动/左右翻页、跳楼、搜索、收藏、正则、身份打码、字体、主题和 CSS；聊天列表长按或点击省略号可备注、改显示名、置顶和重新绑定。

点击底部进度，选择“楼层跳转 / 章节目录”；章节模式每楼单独阅读，目录显示正文摘录，每页 50 章。楼层菜单的“备用回复”可切换当前阅读版本；外观中的“从资源库选择美化”提取聊天区域 CSS 与主题颜色，确认应用后生效，不导入酒馆工具栏布局。依赖酒馆专有 DOM 的样式不保证完整复现。

0.1.7 参考已有美化与开场白预览，补齐消息气泡和头像结构，保留聊天背景图与装饰图；酒馆排版显示角色头像，小说排版隐藏头像并保留读者字号/行距。资源库美化中的 HTTPS 字体样式按远程媒体开关加载，只提取字体定义；加载失败使用后备字体。整站布局、工具栏和底部占位不带入阅读页。横向分页禁用消息气泡的变换效果，避免分栏绘制异常；作者颜色、圆角、渐变仍保留。

聊天原件与角色关联保存在资源库；偏好、备注、进度和收藏沿用原读了么数据空间，独立列入备份/导出的“读了么”，默认勾选。卸载 APP 默认保留数据；主动清除读了么数据不会删除聊天原件。选择导出聊天时自动带上关联角色卡和配套显示正则，以便恢复阅读效果。手选正文背景图片目前只在本次打开中有效。

新收藏记住具体回复编号，切换到其它回复后，点击收藏会切回收藏的版本。旧收藏保留原摘录与楼层，不猜测此前没有记录的回复编号；原聊天变化时提示并保留摘录，不自动跳到可能错误的位置。“备用回复 → 比较两条回复”按当前正则比较两版正文，用增删标记展示差异，不运行状态栏；每版最多比较前 30000 字。

“阅读外观 → 状态栏配色”可选“保留作者配色 / 融入阅读主题”，与正文字号、行距分开。融合模式使面板背景透明，文字跟随正文实际颜色，边线使用低对比色，简化背景图、渐变及阴影；图片元素自身不改色。可以随时恢复作者配色。调整正文字号和行距不重建交互状态栏。

章节模式每次读取一楼，连续模式每批三楼；先预热下一楼的正则结果。交互面板只在当前及邻近阅读区域运行，远处面板释放脚本，返回时按归档快照重新挂载，因此临时展开/切换状态可能重置。正则结果使用有上限的内存缓存，原文、回复、规则或身份上下文变化后重新计算；缓存不保存另一份聊天原件。

阅读显示中三按钮同行，原位切换“纯净 / 精简 / 完整”：纯净只保留小说正文，去掉 HTML 状态栏、媒体和脚本，不构建或加载状态栏；只有 HTML 的楼层显示无正文提示。精简显示静态状态栏，完整可展开、收起和切换人物，不另开预览页。默认让状态栏外层背景融入阅读页，可在显示设置恢复作者背景。横向翻页时正文按一屏分栏，较长 HTML 独占可向下延伸的阅读页；在整张阅读页滚动看完，再左右翻页，不再由阅读器给面板限高或增加内部滚动。作者自身的折叠、边框和内部高度约束仍保留。横向模式使用已有翻页按钮跨章，不再让重复章末导航占出空页；上下滚动仍有章末导航。脚本只能读取当前楼层已保存的变量快照，不重放历史 MVU 更新，也不写回酒馆。隐藏身份时自动回到精简模式，图片文字不保证打码。

“阅读显示 → 管理显示正则”按全局、预设、角色卡分组逐条开关，选择仅保存到当前聊天，可恢复导出时设置；默认保留单条禁用及整组启用状态，按酒馆顺序执行。只重跑显示规则，不重跑已写入原文的输入/输出规则。预设来源标注为本次导出所选预设，不声称是这份旧聊天当时使用的预设。JSONL 本身不包含外部规则，旧测试记录需要通过新候选 Bridge 再次取回才能补齐。

聊天互传只复制已保存的单角色聊天，不保存、切换或删除酒馆当前聊天。重复接收不覆盖已有绑定；卡本身更新后可能形成新卡分组，可手动整理。实时传输的聊天和 PNG 不再分别限制 64/16 MiB，合成归档不超过通用单文件 256 MiB；随附正则不超过 2 MiB；暂存合计不超过 16 MiB，30 分钟过期。

本版尚不包含群聊、目录/SAF 聊天读取、历史 MVU 变量重放或外部窗口跳转。电脑测试包含真实 ST 只读导出、候选 Bridge Adapter/传输和两份实际聊天；扩展未替换到实际酒馆安装目录。实体 iOS/Android、任意外部依赖仍未验证。

设置中的“隐藏聊天记录配套正则”默认开启，只隐藏列表展示，不影响阅读、导出和备份；独立导入的普通正则不会被统一隐藏。聊天导回酒馆默认只传聊天，可选择同时传配套正则；先确认具体接收头像文件，酒馆保存新记录，配套规则作为停用的角色规则副本添加，不覆盖原规则。实时回传需 Bridge 0.3.36-chat.4 的 chat-import-v1 能力；加密暂存两端也会确认目标。

源码位置：`extensions/duleme`。构建：`pnpm build`；可选扩展包：`pnpm package:chat-reader`。相关解析、阅读、备份与互传回归测试随 `pnpm test` 运行。
